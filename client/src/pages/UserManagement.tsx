import { useAuth } from "@/_core/hooks/useAuth";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { trpc } from "@/lib/trpc";
import { formatDate } from "@/lib/format";
import { Restricted } from "./AuditLog";
import { ShieldCheck, UsersRound } from "lucide-react";
import { toast } from "sonner";

export default function UserManagement() {
  const { user } = useAuth(); const isAdmin = user?.role === "admin"; const utils = trpc.useUtils(); const { data: users = [], isLoading } = trpc.users.list.useQuery(undefined, { enabled: isAdmin }); const updateRole = trpc.users.updateRole.useMutation({ onSuccess: () => { utils.users.list.invalidate(); toast.success("Rôle utilisateur mis à jour."); } });
  if (!isAdmin) return <Restricted title="Gestion des utilisateurs" description="Seuls les administrateurs peuvent modifier les niveaux d’accès de l’organisation." />;
  return <div className="mx-auto max-w-[1300px] space-y-6"><header><p className="text-xs font-medium uppercase tracking-[0.16em] text-cyan-300">Organisation</p><h1 className="mt-2 text-2xl font-semibold tracking-tight text-white sm:text-3xl">Utilisateurs & rôles</h1><p className="mt-1.5 text-sm text-slate-400">Les administrateurs configurent l’espace ; les opérateurs enregistrent les mouvements de stock.</p></header><section className="grid gap-4 sm:grid-cols-2"><Card className="border-white/[0.07] bg-[#111722] shadow-none"><CardContent className="flex gap-4 p-5"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-cyan-400/10 text-cyan-300"><ShieldCheck className="h-5 w-5" /></span><div><p className="text-sm font-semibold text-slate-200">Administrateur</p><p className="mt-1 text-xs leading-5 text-slate-500">Gère le catalogue, les fournisseurs, les rôles et les paramètres de suivi.</p></div></CardContent></Card><Card className="border-white/[0.07] bg-[#111722] shadow-none"><CardContent className="flex gap-4 p-5"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-violet-400/10 text-violet-300"><UsersRound className="h-5 w-5" /></span><div><p className="text-sm font-semibold text-slate-200">Opérateur</p><p className="mt-1 text-xs leading-5 text-slate-500">Enregistre les entrées, sorties et ajustements avec une traçabilité complète.</p></div></CardContent></Card></section><Card className="border-white/[0.07] bg-[#111722] shadow-none"><CardContent className="p-0"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left"><thead className="bg-white/[0.018] text-[10px] font-semibold uppercase tracking-[0.13em] text-slate-500"><tr><th className="px-6 py-3">Utilisateur</th><th className="px-4 py-3">Rôle</th><th className="px-4 py-3">Dernière connexion</th><th className="px-6 py-3 text-right">Modifier</th></tr></thead><tbody className="divide-y divide-white/[0.06]">{isLoading ? <tr><td colSpan={4} className="px-6 py-12 text-center text-sm text-slate-500">Chargement des utilisateurs…</td></tr> : users.map(member => <tr key={member.id} className="text-sm hover:bg-white/[0.018]"><td className="px-6 py-4"><p className="font-medium text-slate-200">{member.name || "Utilisateur sans nom"}</p><p className="mt-0.5 text-xs text-slate-500">{member.email || "Adresse non renseignée"}</p></td><td className="px-4 py-4"><Badge className={`border-0 ${member.role === "admin" ? "bg-cyan-400/10 text-cyan-300" : "bg-violet-400/10 text-violet-300"}`}>{member.role === "admin" ? "Administrateur" : "Opérateur"}</Badge></td><td className="px-4 py-4 text-xs text-slate-500">{formatDate(member.lastSignedIn, true)}</td><td className="px-6 py-4 text-right"><Select value={member.role} disabled={member.id === user?.id || updateRole.isPending} onValueChange={role => updateRole.mutate({ id: member.id, role: role as "admin" | "user" }, { onError: error => toast.error(error.message) })}><SelectTrigger className="ml-auto h-8 w-36 text-xs"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="admin">Administrateur</SelectItem><SelectItem value="user">Opérateur</SelectItem></SelectContent></Select></td></tr>)}</tbody></table></div></CardContent></Card></div>;
}
