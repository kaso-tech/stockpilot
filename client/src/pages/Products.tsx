import { useAuth } from "@/_core/hooks/useAuth";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { formatCurrency } from "@/lib/format";
import { trpc } from "@/lib/trpc";
import { Edit3, PackagePlus, RotateCcw, Search, SlidersHorizontal, Trash2 } from "lucide-react";
import { useMemo, useState } from "react";
import { toast } from "sonner";

type ProductForm = { reference: string; name: string; category: string; unit: string; purchasePrice: string; retailPrice: string; wholesalePrice: string; quantity: string; minimumQuantity: string; supplierId: string };
const empty: ProductForm = { reference: "", name: "", category: "", unit: "unité", purchasePrice: "", retailPrice: "", wholesalePrice: "", quantity: "0", minimumQuantity: "0", supplierId: "none" };

export default function Products() {
  const { user } = useAuth(); const canManage = user?.role === "admin"; const utils = trpc.useUtils();
  const { data: products = [], isLoading } = trpc.products.list.useQuery(); const { data: suppliers = [] } = trpc.suppliers.list.useQuery();
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("all"); const [supplier, setSupplier] = useState("all"); const [status, setStatus] = useState("all"); const [open, setOpen] = useState(false); const [editingId, setEditingId] = useState<number | null>(null); const [form, setForm] = useState<ProductForm>(empty);
  const categories = useMemo(() => Array.from(new Set(products.map(item => item.category))).sort(), [products]);
  const filtered = useMemo(() => products.filter(item => [item.name, item.reference, item.category].some(value => value.toLowerCase().includes(query.toLowerCase())) && (category === "all" || item.category === category) && (supplier === "all" || (supplier === "none" ? item.supplierId === null : item.supplierId === Number(supplier))) && (status === "all" || (status === "alert" ? item.quantity <= item.minimumQuantity : item.quantity > item.minimumQuantity))), [products, query, category, supplier, status]);
  const hasFilters = Boolean(query) || category !== "all" || supplier !== "all" || status !== "all";
  const create = trpc.products.create.useMutation({ onSuccess: () => { utils.products.list.invalidate(); utils.dashboard.get.invalidate(); toast.success("Produit créé."); } }); const update = trpc.products.update.useMutation({ onSuccess: () => { utils.products.list.invalidate(); utils.dashboard.get.invalidate(); toast.success("Produit mis à jour."); } }); const remove = trpc.products.remove.useMutation({ onSuccess: () => { utils.products.list.invalidate(); utils.dashboard.get.invalidate(); toast.success("Produit supprimé."); } });
  const reset = () => { setForm(empty); setEditingId(null); }; const clearFilters = () => { setQuery(""); setCategory("all"); setSupplier("all"); setStatus("all"); };
  const edit = (item: typeof products[number]) => { setEditingId(item.id); setForm({ reference: item.reference, name: item.name, category: item.category, unit: item.unit, purchasePrice: (item.purchasePriceCents / 100).toFixed(2), retailPrice: (item.retailPriceCents / 100).toFixed(2), wholesalePrice: (item.wholesalePriceCents / 100).toFixed(2), quantity: String(item.quantity), minimumQuantity: String(item.minimumQuantity), supplierId: item.supplierId ? String(item.supplierId) : "none" }); setOpen(true); };
  const save = () => { const data = { reference: form.reference.trim(), name: form.name.trim(), category: form.category.trim(), unit: form.unit.trim(), purchasePriceCents: Math.round(Number(form.purchasePrice) * 100), retailPriceCents: Math.round(Number(form.retailPrice) * 100), wholesalePriceCents: Math.round(Number(form.wholesalePrice) * 100), quantity: Number(form.quantity), minimumQuantity: Number(form.minimumQuantity), supplierId: form.supplierId === "none" ? null : Number(form.supplierId) }; if (!data.reference || !data.name || !data.category || !data.unit || [data.purchasePriceCents, data.retailPriceCents, data.wholesalePriceCents].some(Number.isNaN)) return toast.error("Renseignez les trois prix et les champs requis."); const done = () => { setOpen(false); reset(); }; editingId ? update.mutate({ id: editingId, ...data }, { onSuccess: done, onError: error => toast.error(error.message) }) : create.mutate(data, { onSuccess: done, onError: error => toast.error(error.message) }); };

  return <div className="mx-auto max-w-[1500px] space-y-6"><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-medium uppercase tracking-[0.16em] text-cyan-300">Catalogue</p><h1 className="mt-2 text-2xl font-semibold tracking-tight text-white sm:text-3xl">Produits & références</h1><p className="mt-1.5 text-sm text-slate-400">Les trois prix permettent d’appliquer automatiquement le bon tarif selon le client.</p></div>{canManage && <Button onClick={() => { reset(); setOpen(true); }} className="h-10 bg-cyan-400 text-slate-950 hover:bg-cyan-300"><PackagePlus className="mr-2 h-4 w-4" /> Ajouter un produit</Button>}</header>
    <Card className="border-white/[0.07] bg-[#111722] shadow-none"><CardContent className="p-0"><div className="border-b border-white/[0.07] p-4"><div className="flex flex-col gap-3 xl:flex-row"><div className="relative min-w-0 flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={event => setQuery(event.target.value)} placeholder="Rechercher une référence…" className="border-white/10 bg-white/[0.03] pl-9" /></div><Select value={category} onValueChange={setCategory}><SelectTrigger className="xl:w-40"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">Toutes catégories</SelectItem>{categories.map(value => <SelectItem key={value} value={value}>{value}</SelectItem>)}</SelectContent></Select><Select value={supplier} onValueChange={setSupplier}><SelectTrigger className="xl:w-44"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">Tous fournisseurs</SelectItem><SelectItem value="none">Sans fournisseur</SelectItem>{suppliers.map(item => <SelectItem key={item.id} value={String(item.id)}>{item.name}</SelectItem>)}</SelectContent></Select><Select value={status} onValueChange={setStatus}><SelectTrigger className="xl:w-40"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">Tous statuts</SelectItem><SelectItem value="alert">Sous le seuil</SelectItem><SelectItem value="healthy">Stock sain</SelectItem></SelectContent></Select>{hasFilters && <Button onClick={clearFilters} variant="ghost" className="text-slate-400"><RotateCcw className="mr-2 h-4 w-4" />Réinitialiser</Button>}</div></div>{isLoading ? <div className="px-6 py-16 text-center text-sm text-slate-500">Chargement du catalogue…</div> : !filtered.length ? <div className="px-6 py-16 text-center"><SlidersHorizontal className="mx-auto h-6 w-6 text-slate-600" /><p className="mt-3 font-medium text-slate-300">Aucune référence trouvée</p><p className="mt-1 text-xs text-slate-500">Modifiez vos filtres ou ajoutez un produit.</p></div> : <div className="overflow-x-auto"><table className="w-full min-w-[1040px] text-left"><thead className="bg-white/[0.018] text-[10px] font-semibold uppercase tracking-[0.13em] text-slate-500"><tr><th className="px-6 py-3">Produit</th><th className="px-4 py-3">Achat</th><th className="px-4 py-3">Détail</th><th className="px-4 py-3">Gros</th><th className="px-4 py-3">Stock</th><th className="px-6 py-3 text-right">Actions</th></tr></thead><tbody className="divide-y divide-white/[0.06]">{filtered.map(item => { const low = item.quantity <= item.minimumQuantity; return <tr key={item.id} className="text-sm hover:bg-white/[0.018]"><td className="px-6 py-4"><p className="font-medium text-slate-100">{item.name}</p><p className="mt-0.5 text-xs text-slate-500">{item.reference} · {item.category}</p></td><td className="px-4 py-4 text-slate-400">{formatCurrency(item.purchasePriceCents)}</td><td className="px-4 py-4 font-medium text-cyan-200">{formatCurrency(item.retailPriceCents)}</td><td className="px-4 py-4 font-medium text-violet-200">{formatCurrency(item.wholesalePriceCents)}</td><td className="px-4 py-4"><Badge className={`border-0 ${low ? "bg-amber-400/10 text-amber-300" : "bg-emerald-400/10 text-emerald-300"}`}>{item.quantity} {item.unit}</Badge></td><td className="px-6 py-4 text-right">{canManage && <div className="flex justify-end gap-1"><Button size="icon" variant="ghost" onClick={() => edit(item)} className="h-8 w-8 text-slate-400"><Edit3 className="h-4 w-4" /></Button><Button size="icon" variant="ghost" onClick={() => { if (window.confirm(`Supprimer ${item.name} ?`)) remove.mutate({ id: item.id }, { onError: error => toast.error(error.message) }); }} className="h-8 w-8 text-rose-300"><Trash2 className="h-4 w-4" /></Button></div>}</td></tr>; })}</tbody></table></div>}</CardContent></Card>
    <Dialog open={open} onOpenChange={value => { setOpen(value); if (!value) reset(); }}><DialogContent className="max-h-[90vh] overflow-y-auto border-white/10 bg-[#141b27] text-slate-100 sm:max-w-xl"><DialogHeader><DialogTitle>{editingId ? "Modifier le produit" : "Nouveau produit"}</DialogTitle><DialogDescription className="text-slate-400">Les prix détail et gros sont utilisés automatiquement au POS.</DialogDescription></DialogHeader><div className="grid gap-4 py-2 sm:grid-cols-2"><Field label="Référence *"><Input value={form.reference} onChange={event => setForm({ ...form, reference: event.target.value })} /></Field><Field label="Nom *"><Input value={form.name} onChange={event => setForm({ ...form, name: event.target.value })} /></Field><Field label="Catégorie *"><Input value={form.category} onChange={event => setForm({ ...form, category: event.target.value })} /></Field><Field label="Unité *"><Input value={form.unit} onChange={event => setForm({ ...form, unit: event.target.value })} /></Field><Field label="Prix d’achat *"><Input type="number" step="0.01" value={form.purchasePrice} onChange={event => setForm({ ...form, purchasePrice: event.target.value })} /></Field><Field label="Prix détail *"><Input type="number" step="0.01" value={form.retailPrice} onChange={event => setForm({ ...form, retailPrice: event.target.value })} /></Field><Field label="Prix gros *"><Input type="number" step="0.01" value={form.wholesalePrice} onChange={event => setForm({ ...form, wholesalePrice: event.target.value })} /></Field><Field label="Stock initial *"><Input type="number" min="0" value={form.quantity} onChange={event => setForm({ ...form, quantity: event.target.value })} /></Field><Field label="Seuil minimum *"><Input type="number" min="0" value={form.minimumQuantity} onChange={event => setForm({ ...form, minimumQuantity: event.target.value })} /></Field><Field label="Fournisseur"><Select value={form.supplierId} onValueChange={value => setForm({ ...form, supplierId: value })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="none">Aucun fournisseur</SelectItem>{suppliers.map(item => <SelectItem key={item.id} value={String(item.id)}>{item.name}</SelectItem>)}</SelectContent></Select></Field></div><DialogFooter><Button variant="ghost" onClick={() => setOpen(false)}>Annuler</Button><Button onClick={save} disabled={create.isPending || update.isPending} className="bg-cyan-400 text-slate-950">{editingId ? "Enregistrer" : "Créer"}</Button></DialogFooter></DialogContent></Dialog>
  </div>;
}

function Field({ label, children }: { label: string; children: React.ReactNode }) { return <div className="space-y-2"><Label className="text-xs text-slate-300">{label}</Label>{children}</div>; }
