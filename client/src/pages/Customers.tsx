import { useAuth } from "@/_core/hooks/useAuth";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { trpc } from "@/lib/trpc";
import { Building2, Plus, UsersRound } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";

export default function Customers() {
  const { user } = useAuth(); const isAdmin = user?.role === "admin"; const utils = trpc.useUtils(); const { data: customers = [] } = trpc.commerce.customers.list.useQuery();
  const [open, setOpen] = useState(false); const [form, setForm] = useState({ name: "", type: "ordinary" as "ordinary" | "wholesale", contactName: "", email: "", phone: "", address: "" });
  const create = trpc.commerce.customers.create.useMutation({ onSuccess: () => { utils.commerce.customers.list.invalidate(); setOpen(false); setForm({ name: "", type: "ordinary", contactName: "", email: "", phone: "", address: "" }); toast.success("Client créé."); } });
  const save = () => { if (!form.name.trim()) return toast.error("Le nom du client est requis."); create.mutate({ ...form, name: form.name.trim(), contactName: form.contactName.trim() || null, email: form.email.trim() || null, phone: form.phone.trim() || null, address: form.address.trim() || null }, { onError: error => toast.error(error.message) }); };
  return <div className="mx-auto max-w-[1300px] space-y-6"><header className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-medium uppercase tracking-[0.16em] text-cyan-300">Ventes</p><h1 className="mt-2 text-2xl font-semibold text-white sm:text-3xl">Clients & tarifs</h1><p className="mt-1.5 text-sm text-slate-400">Le type de client détermine automatiquement le prix appliqué au point de vente.</p></div>{isAdmin && <Button onClick={() => setOpen(true)} className="bg-cyan-400 text-slate-950 hover:bg-cyan-300"><Plus className="mr-2 h-4 w-4" /> Nouveau client</Button>}</header><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{customers.length ? customers.map(customer => <Card key={customer.id} className="border-white/[0.07] bg-[#111722] shadow-none"><CardContent className="p-5"><span className={`grid h-10 w-10 place-items-center rounded-xl ${customer.type === "wholesale" ? "bg-violet-400/10 text-violet-300" : "bg-cyan-400/10 text-cyan-300"}`}>{customer.type === "wholesale" ? <Building2 className="h-5 w-5" /> : <UsersRound className="h-5 w-5" />}</span><div className="mt-4 flex items-start justify-between gap-3"><div><p className="font-semibold text-slate-100">{customer.name}</p><p className="mt-1 text-xs text-slate-500">{customer.contactName || "Contact non renseigné"}</p></div><Badge className={`border-0 ${customer.type === "wholesale" ? "bg-violet-400/10 text-violet-200" : "bg-cyan-400/10 text-cyan-200"}`}>{customer.type === "wholesale" ? "Grossiste" : "Ordinaire"}</Badge></div><div className="mt-5 border-t border-white/[0.07] pt-4 text-xs text-slate-500"><p>{customer.email || "E-mail non renseigné"}</p><p className="mt-1">{customer.phone || "Téléphone non renseigné"}</p></div></CardContent></Card>) : <Card className="col-span-full border-dashed border-white/10 bg-[#111722]"><CardContent className="py-16 text-center text-slate-500">Aucun client enregistré.</CardContent></Card>}</div><Dialog open={open} onOpenChange={setOpen}><DialogContent className="border-white/10 bg-[#141b27] text-slate-100"><DialogHeader><DialogTitle>Nouveau client</DialogTitle></DialogHeader><div className="grid gap-4"><Field label="Nom *"><Input value={form.name} onChange={event => setForm({ ...form, name: event.target.value })} /></Field><Field label="Type de client"><Select value={form.type} onValueChange={value => setForm({ ...form, type: value as typeof form.type })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="ordinary">Ordinaire · prix détail</SelectItem><SelectItem value="wholesale">Grossiste · prix gros</SelectItem></SelectContent></Select></Field><Field label="Contact"><Input value={form.contactName} onChange={event => setForm({ ...form, contactName: event.target.value })} /></Field><Field label="E-mail"><Input type="email" value={form.email} onChange={event => setForm({ ...form, email: event.target.value })} /></Field><Field label="Téléphone"><Input value={form.phone} onChange={event => setForm({ ...form, phone: event.target.value })} /></Field></div><DialogFooter><Button variant="ghost" onClick={() => setOpen(false)}>Annuler</Button><Button onClick={save} disabled={create.isPending} className="bg-cyan-400 text-slate-950">Créer</Button></DialogFooter></DialogContent></Dialog></div>;
}
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <div className="space-y-2"><Label className="text-xs text-slate-300">{label}</Label>{children}</div>; }
