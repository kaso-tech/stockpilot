import { CheckoutSheet } from "@/components/CheckoutSheet";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { formatCurrency } from "@/lib/format";
import { trpc } from "@/lib/trpc";
import { Minus, Plus, Search, ShoppingCart, Trash2 } from "lucide-react";
import { useMemo, useState } from "react";
import { toast } from "sonner";

type CartItem = { productId: number; quantity: number };
export default function PointOfSale() {
  const utils = trpc.useUtils(); const { data: products = [] } = trpc.products.list.useQuery(); const { data: agents = [] } = trpc.commerce.agents.list.useQuery(); const { data: settings } = trpc.commerce.settings.get.useQuery();
  const [query, setQuery] = useState(""); const [cart, setCart] = useState<CartItem[]>([]); const [cartOpen, setCartOpen] = useState(false); const [checkoutSale, setCheckoutSale] = useState<{ id: number; totalCents: number; invoiceNumber: string } | null>(null);
  const available = useMemo(() => products.filter(product => product.quantity > 0 && [product.name, product.reference].some(value => value.toLowerCase().includes(query.toLowerCase()))), [products, query]);
  const lines = cart.map(item => { const product = products.find(value => value.id === item.productId); return product ? { ...item, product, totalCents: product.retailPriceCents * item.quantity } : null; }).filter(Boolean) as Array<CartItem & { product: typeof products[number]; totalCents: number }>;
  const totalCents = lines.reduce((sum, line) => sum + line.totalCents, 0);
  const add = (productId: number) => setCart(current => { const product = products.find(item => item.id === productId); const item = current.find(value => value.productId === productId); if (!product) return current; return item ? current.map(value => value.productId === productId ? { ...value, quantity: Math.min(value.quantity + 1, product.quantity) } : value) : [...current, { productId, quantity: 1 }]; });
  const quantity = (productId: number, next: number) => setCart(current => next <= 0 ? current.filter(item => item.productId !== productId) : current.map(item => item.productId === productId ? { ...item, quantity: Math.min(next, products.find(product => product.id === productId)?.quantity ?? next) } : item));
  const draft = trpc.transactions.createDraft.useMutation({ onSuccess: result => { utils.transactions.list.invalidate(); setCheckoutSale(result); setCartOpen(false); }, onError: error => toast.error(error.message) });
  const openCheckout = () => { if (!cart.length) return toast.error("Ajoutez au moins un produit au panier."); draft.mutate({ channel: "pos", customerId: null, salesAgentId: null, cashierId: null, note: null, items: cart }); };
  return <div className="mx-auto max-w-[1600px] space-y-5"><header className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-[#007B8B]">Vente comptoir</p><h1 className="mt-2 text-2xl font-semibold text-slate-950 dark:text-white sm:text-3xl">Point de vente</h1></div><Button onClick={() => setCartOpen(true)} className="bg-[#007B8B] text-white hover:bg-[#006976]"><ShoppingCart className="mr-2 h-4 w-4" />Panier · {cart.length}</Button></header><section className="rounded-2xl border border-border bg-card p-5"><div className="relative max-w-xl"><Search className="pointer-events-none absolute left-3 top-3 h-4 w-4 text-muted-foreground" /><Input value={query} onChange={event => setQuery(event.target.value)} placeholder="Rechercher un produit ou une référence" className="pl-9" /></div><div className="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{available.map(product => <button key={product.id} onClick={() => add(product.id)} className="rounded-xl border border-border p-4 text-left transition hover:border-[#007B8B]/40 hover:bg-[#007B8B]/5"><p className="truncate font-medium">{product.name}</p><p className="mt-1 text-xs text-muted-foreground">{product.reference} · {product.quantity} {product.unit}</p><div className="mt-4 flex items-center justify-between"><span className="font-semibold text-[#007B8B]">{formatCurrency(product.retailPriceCents)}</span><Plus className="h-4 w-4" /></div></button>)}</div>{!available.length && <p className="py-14 text-center text-sm text-muted-foreground">Aucun produit disponible.</p>}</section><CheckoutSheet open={Boolean(checkoutSale)} onOpenChange={open => !open && setCheckoutSale(null)} sale={checkoutSale} agents={agents} settings={settings} onComplete={() => { setCart([]); setCheckoutSale(null); utils.products.list.invalidate(); }} /><div className={`fixed inset-x-0 bottom-0 z-30 mx-auto max-w-2xl p-4 transition ${cartOpen ? "pointer-events-none opacity-0" : ""}`}><button onClick={() => setCartOpen(true)} className="flex w-full items-center justify-between rounded-2xl bg-[#007B8B] px-5 py-4 text-left text-white shadow-2xl"><span className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-white/15"><ShoppingCart className="h-5 w-5" /></span><span><b>{cart.length} article{cart.length > 1 ? "s" : ""}</b><small className="block text-white/75">Panier flottant</small></span></span><b>{formatCurrency(totalCents)}</b></button></div>{cartOpen && !checkoutSale && <div className="fixed inset-y-0 right-0 z-40 flex w-full max-w-md flex-col bg-[#111722] text-white shadow-2xl"><div className="flex items-center justify-between border-b border-white/10 p-5"><div><h2 className="font-semibold">Panier</h2><p className="text-xs text-slate-400">{cart.length} article{cart.length > 1 ? "s" : ""}</p></div><Button size="icon" variant="ghost" onClick={() => setCartOpen(false)}><Trash2 className="h-4 w-4" /></Button></div><div className="flex-1 divide-y divide-white/[0.07] overflow-y-auto">{lines.map(line => <div key={line.productId} className="p-5"><div className="flex justify-between gap-3"><div><p className="font-medium">{line.product.name}</p><p className="text-xs text-slate-400">{formatCurrency(line.product.retailPriceCents)}</p></div><p className="font-semibold">{formatCurrency(line.totalCents)}</p></div><div className="mt-3 flex items-center gap-2"><Button size="icon" variant="outline" onClick={() => quantity(line.productId, line.quantity - 1)}><Minus className="h-4 w-4" /></Button><span className="w-8 text-center">{line.quantity}</span><Button size="icon" variant="outline" onClick={() => quantity(line.productId, line.quantity + 1)}><Plus className="h-4 w-4" /></Button></div></div>)}</div><div className="border-t border-white/10 p-5"><div className="mb-4 flex justify-between text-xl font-semibold"><span>Total</span><span>{formatCurrency(totalCents)}</span></div><Button onClick={openCheckout} disabled={draft.isPending || !cart.length} className="w-full bg-[#007B8B] text-white hover:bg-[#006976]">Encaisser</Button></div></div>}</div>;
}
